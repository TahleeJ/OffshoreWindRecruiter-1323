import React from 'react';
import { RecommendedJobs } from '../../firebase/Types';
import { useAppSelector } from '../../redux/hooks';

interface props {

}

const SurveyResponse: React.FC<props> = p => {


    return (
        <div>Reponse</div>
    )
}

export default SurveyResponse;